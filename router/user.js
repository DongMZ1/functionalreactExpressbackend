const express = require('express');
const { check } = require('express-validator');

const placesControllers = require('../controller/user-controller');

const router = express.Router();

module.exports = router;